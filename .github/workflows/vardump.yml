on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Test Itself
    steps:
    - name: Check 1
      id: check1
      uses: umutphp/php-var-dump-check-action@master
      with:
        checktype: 'none'
        exclude: 'vendor,test'
        extensions: 'php'